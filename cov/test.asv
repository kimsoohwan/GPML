clc
clear all
close all

% hyperparameters
hyp = log([1, 1]);

% data
d = 3;
n = 5;  x = rand(n, d);
nd = 
xd = [

z = [
x = rand
x = [1, 2, 3]';
z = [-1, 0, 1]';

% combinations
for i = 0:length(hyp)
    for pdx = 0:length(x)
        for pdz = 0:length(z)
            k = covSEisoDiffSlow(hyp, x, z, i, pdx, pdz);
        end
    end
end

